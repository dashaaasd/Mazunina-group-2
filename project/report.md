# Отчёт по итоговому проекту по курсу «Инженерия Искусственного Интеллекта»

---

## 1. Паспорт проекта

- **Название проекта:** ML-сервис прогнозирования эффективности рекламных кампаний (ROAS Prediction Service)
- **Автор:** Мазунина Дарья Андреевна
- **Группа:** БСБО-51-24
- **Контакт:** @dm_dmaz
- **Ссылка на репозиторий:** https://github.com/dashaaasd/Mazunina-group-2

Проект представляет собой ML-сервис для прогнозирования ROAS (Return on Ad Spend) рекламных кампаний и рекомендации наиболее эффективных рекламных каналов. Сервис на основе исторических данных о кампаниях предсказывает окупаемость вложений в рекламу и помогает выбрать оптимальную платформу для размещения. Результат — REST API на FastAPI с двумя эндпоинтами.

---

## 2. Постановка задачи и контекст

Опишите:

1. **Предметную область и задачу:**
   - Задача: регрессия — прогнозирование числового значения ROAS по параметрам рекламной кампании.
   - Пользователь: маркетолог, планирующий рекламную кампанию. Сценарий использования:
      1. Пользователь вводит параметры планируемой кампании (платформа, бюджет, цель, устройство, дата)
      2. Сервис прогнозирует ROAS — сколько рублей выручки принесёт каждый рубль затрат
      3. Сервис рекомендует лучший канал для максимизации окупаемости

2. **Формулировку задачи в терминах ML/ИИ:**
   - Входные данные: 40 признака — метрики (CTR, bounce_rate, conversions, ad_spend, quality_score), категориальные (platform, campaign_objective, device_type, operating_system, ad_placement, day_of_week), временные (month, quarter, hour_of_day, is_weekend)
   - Целевая переменная: ROAS (Return on Ad Spend)
   - Требования: предсказание в реальном времени (<1 сек), интерпретируемость (feature importance), устойчивость к временному дрейфу данных

3. **Целевые метрики качества:**

| Метрика | Обоснование |
|---------|-------------|
| **R²** | Основная — измеряет долю объяснённой дисперсии, стандарт для регрессии |
| **MAE** | Средняя абсолютная ошибка в единицах ROAS — интерпретируема для бизнеса |
| **RMSE** | Штрафует большие ошибки сильнее — важно для высоких ROAS |
| **MAPE** | Процентная ошибка — позволяет сравнивать точность на разных масштабах |

---

## 3. Данные

Опишите, какие данные использует проект:

1. **Источник данных:**
   **Датасет:** Digital Advertising Campaign Performance Dataset ([Kaggle](https://www.kaggle.com/datasets/manishabhatt22/marketing-campaign-performance-dataset)). 10 000 записей о рекламных кампаниях за 2024–2026 годы. Данные синтезированы, но при этом там есть бизнес-закономерности.

2. **Структура данных:**

| Группа признаков | Примеры | Тип |
|------------------|---------|-----|
| Метрики эффективности | CTR, bounce_rate, conversions, quality_score, actual_cpc | Числовые |
| Расходы | ad_spend | Числовые |
| Каналы | platform (6 значений), ad_placement (6 значений) | Категориальные |
| Контекст | device_type (3), operating_system (5), campaign_objective (5) | Категориальные |
| Временные | start_date, quarter, day_of_week, hour_of_day, campaign_day | Дата/числовые |

3. **Предобработка и EDA:**
   **Ключевые шаги (`preproseccor.py`):**
   - Удаление утечек: `revenue`, `profit` (ROAS = revenue / ad_spend)
   - Извлечение временных признаков из `start_date`: `year`, `month`, `week_of_year`, `is_weekend`
   - Кодирование категорий: LabelEncoder для 6 признаков
   - Масштабирование числовых признаков: StandardScaler

   **Важные наблюдения EDA:(`notebooks/01_eda_v2.ipynb`)**
   - ROAS имеет сильный скос вправо (среднее 7.52, максимум 487) — есть редкие "звёздные" кампании
   - **LinkedIn лидирует по ROAS** (7.5x), но Facebook/Instagram дают больший абсолютный profit
   - **Desktop стабильно эффективнее Mobile** на ~30%
   - **Сезонность:** ROAS колеблется по месяцам от 6.3 до 8.9x (разница ~40%)
   - **Ключевые факторы:**
   - Позитивные: conversions (+0.30), CTR (+0.25), pages_per_session (+0.28)
   - Негативные: bounce_rate (−0.34), actual_cpc (−0.24)
   - **Бюджет сам по себе не гарантирует результат:** ad_spend (−0.04) — почти не коррелирует с ROAS

---

## 4. Модели и подходы

Опишите, какие модели вы пробовали и как развивался ваш подход:


1. **Базовые (baseline) модели:(ноутбук `02_baseline_models.ipynb`)**

| Модель | Val R² | Val MAE | Вывод |
|--------|--------|---------|-------|
| Linear Regression | 0.249 | 5.41 | Слабо — только линейные зависимости |
| Ridge (L2) | 0.249 | 5.41 | Не лучше линейной |
| Lasso (L1) | 0.258 | 5.41 | Незначительно лучше |
| Decision Tree | −0.186 | 4.94 | Переобучен |
| Random Forest | 0.526 | 3.32 | Хорошо |
| Gradient Boosting | 0.521 | 3.25 | Хорошо |
| **CatBoost** | **0.595** | **3.23** | **Лучшая** |

2. **Улучшенные модели и эксперименты:(ноутбук `03_hyperparameter_tuning.ipynb`)**
   **Методы:**
   1. RandomizedSearchCV с TimeSeriesSplit (5 фолдов) — 50 комбинаций
   2. Ручной перебор с фокусом на сильную регуляризацию (l2_leaf_reg до 20)

   **Результаты**: улучшений не произошло (Baseline CatBoost: Val R² - 0.882, Tuned CatBoost: Val R² - 0.879), что говорит о том что изначальные параметры оптимальны

		**Итоговые параметры**: iterations=500, learning_rate=0.05, depth=6

---

## 5. Экспериментальный протокол и результаты 55

Здесь важно показать, что выбор финальной модели был осмысленным.

1. **Экспериментальный протокол:**
   - как вы делили выборку (train/val/test, k-fold и т.п.);
   - как считали метрики (на какой части данных, с какими параметрами).

2. **Сравнение моделей по метрикам:**

| Модель | описание | Val R² | Val MAE | Комментарий |
|--------|---------|---------|-------|
| Linear Regression | 0.249 | 5.41 | Слабо — только линейные зависимости |
| Ridge (L2) | 0.249 | 5.41 | Не лучше линейной |
| Lasso (L1) | 0.258 | 5.41 | Незначительно лучше |
| Decision Tree | −0.186 | 4.94 | Переобучен |
| Random Forest | 0.526 | 3.32 | Хорошо |
| Gradient Boosting | 0.521 | 3.25 | Хорошо |
| **CatBoost** | **0.595** | **3.23** | **Лучшая** |

3. **Выбор финальной модели:**
   - какая модель выбрана как финальная и почему (баланс точности, скорости, сложности);
   - какие trade-off’ы приходилось учитывать.

---

## 6. Архитектура решения и сервис

Опишите, как из модели получился работающий сервис:

1. **Архитектура пайплайна:**
   - как связаны между собой загрузка данных, предобработка, модель и сервис;
   - можно приложить простую схему (текстом или картинку).

2. **API и endpoints:**
   - какие endpoints реализованы (минимум: `/health`, `/predict`);
   - какие параметры принимает `/predict` и что возвращает.

3. **Технологический стек:**
   - какие библиотеки и фреймворки используются;
   - используется ли Docker, какие есть команды для поднятия сервиса.

---

## 7. Наблюдаемость, конфигурация и безопасность

Кратко опишите:

1. **Логи и наблюдаемость:**
   - что логируется (запросы, ошибки, важные события);
   - как можно понять, что сервис работает корректно.

2. **Конфигурации:**
   - какие параметры вынесены в конфиги (`configs/`, `.env.example`);
   - что можно перенастроить без изменения кода.

3. **Безопасность:**
   - как вы избегали коммита секретов (токенов, паролей);
   - нет ли в репозитории “чувствительных” данных.

---

## 8. Ограничения и дальнейшая работа

Будьте честны:

- какие у проекта есть ограничения (качество, скорость, устойчивость, объём данных и т.п.);
- что бы вы сделали, если бы было больше времени:
  - улучшения модели;
  - улучшения сервиса;
  - улучшения данных и экспериментов.

---

## 9. Сценарий демонстрации на защите

Опишите, как вы планируете демонстрировать проект:

1. Что вы **запускаете**:
   - сервис, ноутбук, скрипт и т.п.;
2. Какие **1-2 ключевых сценария** вы показываете:
   - пример запроса к `/predict`;
   - пример работы с реальными/демонстрационными данными;
3. На что хотите, чтобы преподаватель обратил внимание:
   - качество предсказаний;
   - устойчивость;
   - удобство API;
   - архитектуру.

Ссылайтесь на команды и инструкции из `project/README.md`.

---
