# Итоговый проект по курсу «Инженерия Искусственного Интеллекта»

В этой папке находится итоговый мини-проект по курсу.  
Проект должен демонстрировать применение методов и инструментов инженерии ИИ: работу с данными, модели, пайплайны, сервис, эксперименты и (по возможности) воспроизводимость.

Заполните данный файл под ваш конкретный проект.

---

## 1. Паспорт проекта

- **Название проекта:** Предсказание эффективности рекламного канала для сегментов аудитории
- **Автор:** Мазунина Дарья Андреевна
- **Группа:** БСБО-51-24
- **Контакт:** @dm_dmaz

- **Краткое описание (2-4 предложения):**  

<<<<<<< HEAD
Проект посвящён построению ML-сервиса для рекомендации наиболее эффективных рекламных каналов на основе исторических данных о маркетинговых кампаниях. Используются агрегированные данные по кампаниям (бюджет, охваты, конверсии, метрики эффективности), модели градиентного бустинга (CatBoost/XGBoost) для прогнозирования и логистическая регрессия для интерпретации результатов. Результат — REST API, который по параметрам планируемой кампании (канал, бюджет, временной контекст) возвращает прогноз эффективности (ROAS) и рекомендацию по выбору канала для максимизации отклика.
=======
Проект представляет собой ML-сервис для прогнозирования ROAS (Return on Ad Spend) рекламных кампаний и рекомендации наиболее эффективных рекламных платформ. На основе исторических данных о 10 000 кампаний модель CatBoost предсказывает окупаемость вложений и ранжирует каналы по эффективности. Результат — REST API на FastAPI с эндпоинтами `/predict` и `/recommend`, документация Swagger, контейнеризация Docker.
>>>>>>> ba01acf7020b8b2fe37dfb8ecd3f595173818217

---

## 2. Структура проекта

Проект организован в следующей структуре:
```

project/
├── requirements.txt          # зависимости проекта
├── report.md                 # отчёт по проекту
├── self-checklist.md         # чеклист самопроверки проекта перед сдачей
├── Dockerfile                # контейнеризация
├── .dockerignore
├── notebooks/                # экспериментальные ноутбуки             
│   ├── 01_eda.ipynb                    # разведочный анализ
│   ├── 02_baseline_models.ipynb        # базовые модели (7 моделей)
│   └── 03_hyperparameter_tuning.ipynb  # подбор гиперпараметров MLflow
├── src/                      # основной код проекта
│   ├── train.py                        # обучение модели
│   ├── data/
│   │   └── preprocessor.py            # предобработка
│   ├── models/
│   │   └── predictor.py               # загрузка + predict/recommend
│   └── api/
│       ├── app.py                      # FastAPI сервер
│       └── schemas.py                  # Pydantic модели
├── data/                     # демонстрационные/учебные данные
│   └── tech_advertising_campaigns_dataset.csv
├── configs/                  # конфигурационные файлы
│   ├── model_config.yaml              # параметры модели
│   └── .env.example                   # шаблон (секретов нет)
├── tests/                    # тесты
│   ├── test_preprocessor.py
│   ├── test_predictor.py
│   └── test_api.py
└── artifacts/                # сохранненные модели, отчеты, результаты экспериментов
				├── models/                         # обученные модели (.pkl)
				└── final_metrics.json             # финальные метрики

```

---

## 3. Требования и установка

### 3.1. Требования

- Python `>= 3.10` (уточните при необходимости).
- Docker (оптионально, для контейнеризации).

### 3.2. Установка окружения

```bash
# Перейти в папку проекта
cd project

# Создать виртуальное окружение (опционально, но рекомендуется)
python -m venv .venv

# Активировать окружение:
# Windows:
.venv\Scripts\activate
# Linux / macOS:
source .venv/bin/activate

# Установить зависимости
pip install --upgrade pip
pip install -r requirements.txt
```

---

## 4. Как запустить проект

### 4.1. Запуск обучения модели

```bash
cd project
source .venv/bin/activate      # при необходимости
python -m src.train            # или другая команда
```
Модель обучится и сохранится в artifacts/models/best_model_service.pkl.

### 4.2. Запуск сервиса (API/веб-интерфейс)

```bash
<<<<<<< HEAD


source .venv/bin/activate      # при необходимости
uvicorn src.api.app:app --reload --port 8000         # пример: FastAPI/Flask сервис
```
docker build -t roas-service .
docker run -p 8000:8000 roas-service
Или, если используется Docker:
=======
cd project
source .venv/bin/activate      # при необходимости
python -m src.service          # пример: FastAPI/Flask сервис
```
Порт: 8000

Эндпоинты:

Метод Путь Описание
GET /health Проверка работоспособности
POST /predict Прогноз ROAS для кампании
POST /recommend Рекомендация лучшего канала

Swagger: http://localhost:8000/docs


4.3. Запуск через Docker
>>>>>>> ba01acf7020b8b2fe37dfb8ecd3f595173818217

```bash
cd project
docker build -t roas-service .
docker run -p 8000:8000 roas-service
```
Браузер: http://localhost:8000/docs

<<<<<<< HEAD
Health: http://localhost:8000/health
Опишите:
=======
4.4. Тестирование API
>>>>>>> ba01acf7020b8b2fe37dfb8ecd3f595173818217

```powershell
# Health
Invoke-RestMethod -Uri "http://127.0.0.1:8000/health"

# Predict
Invoke-RestMethod -Uri "http://127.0.0.1:8000/predict" -Method Post -ContentType "application/json" -Body '{"platform":"Facebook","campaign_objective":"Conversions","device_type":"Desktop","operating_system":"Windows","ad_placement":"feed","day_of_week":"Monday","ad_spend":5000,"start_date":"2025-01-15"}'

# Recommend
Invoke-RestMethod -Uri "http://127.0.0.1:8000/recommend" -Method Post -ContentType "application/json" -Body '{"campaign_objective":"Conversions","device_type":"Desktop","operating_system":"Windows","ad_placement":"feed","day_of_week":"Monday","ad_spend":5000,"start_date":"2025-01-15"}'
```

---

## 5. Данные

Кратко опишите используемые данные:

- Источник: Digital Advertising Campaign Performance Dataset (Kaggle) (*добавить ссылку*)
	Тип: синтетические данные с реалистичными бизнес-правилами
- Файл: data/tech_advertising_campaigns_dataset.csv
В data лежит полная версия датасета, ссылка и более подробное описание находится в `data/data_deescription.md`

---

## 6. Тесты (если есть)(поправить код и добавить описание)

```bash
cd project
pytest tests -v
```

Что проверяется:

· test_preprocessor.py — препроцессор не падает, утечек нет, transform работает
· test_predictor.py — модель загружается, predict возвращает ROAS > 0, recommend возвращает 6 каналов
· test_api.py — /health отвечает 200, /predict возвращает predicted_ROAS, /recommend возвращает best_channel

---

## 7. Демонстрация на защите(это продумать и поправить)

1. Структура проекта: покажу src/, notebooks/, configs/, artifacts/
2. Запуск сервиса: uvicorn src.api.app:app --reload --port 8000
3. Swagger UI: http://localhost:8000/docs — демонстрация /predict и /recommend
4. Ноутбук с EDA: ключевые графики и выводы (сезонность, корреляции, платформы)
5. Сравнение моделей: таблица baseline-моделей, R² CatBoost = 0.882
6. Feature Importance: CPA (33%), conversion_rate (22.5%), income_bracket (11.5%)

---

## 8. Ограничения и дальнейшая работа(это тоже поправить)

Ограничения:

· Данные синтетические — требуется валидация на реальных кампаниях
· Модель обучена на 2024–2026 — требуется периодическое переобучение
· Только 6 рекламных платформ — расширение требует дообучения

Дальнейшая работа:

· A/B-тестирование на реальных данных
· Добавление новых платформ и каналов
· Логирование ROAS для снижения MAPE
· Кэширование предсказаний для частых запросов
· Мониторинг дрейфа данных и автоматическое переобучение

---

## 9. Оценка проекта

Итоговая оценка за проект выставляется по пятибалльной шкале (2-5).

Ориентиры для оценки:

- **2** – проект не принят:
  - не выполняются минимальные требования (сервис не запускается, отсутствует ключевой функционал);
  - грубые нарушения требований курса;
  - явный плагиат или формальная имитация работы.
- **3** – проект принят, но реализован на базовом уровне:
  - минимальный функционал есть;
  - по чеклисту `self-checklist.md` выполнено **меньше 5** пунктов;
  - эксперименты и документация слабо проработаны.
- **4** – хороший, рабочий проект:
  - сервис запускается по `README.md`, `/predict` использует реальную модель;
  - есть данные, EDA и эксперименты с метриками;
  - структура кода и конфигураций в целом адекватна;
  - по чеклисту выполнено **не менее 5** пунктов.
- **5** – сильный, хорошо проработанный проект:
  - аккуратно реализован сервис и пайплайн;
  - проведены осмысленные эксперименты и обоснован выбор финальной модели;
  - есть базовая наблюдаемость и работа с конфигами/секретами;
  - документация позволяет быстро понять и воспроизвести решение;
  - по чеклисту выполнено **не менее 9** пунктов.

Чеклист `self-checklist.md` служит для самопроверки студента и как подсказка при проверке.  
Окончательное решение по оценке остаётся за преподавателем и может учитывать:

- качество реализации внутри каждого пункта чеклиста;
- дополнительные сильные стороны проекта (нестандартные решения, дополнительные функции, продвинутая ML-часть и т.п.);
- соблюдение требований курса и дедлайнов.

---
